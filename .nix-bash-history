git rm -f .nix-bash-history
rm container-listing.txt 
git status .
git add poetry.lock requirements.txt .nix-bash-history
git diff --cached Justfile
git commit Justfile -m "Do more thorough clean after sync-upstream"
git status .
git commit .gitignore -m "Igore nix result folders"
git commit nixos/docs/* -m "Add Prisma AI analysis session logs"
git commit nixos/prisma* -m "feat(prisma): Repackage prisma 5.4.2 and prisma-engines 5.4.2 as Nix packages for use in flake.nix"
git status .
git diff --cached flake.nix
git status .
git commit flake.lock flake.nix -m "feat(prisma):Replaced Nix Prisma 5.22.0 package with custom Prisma 5.4.2 package"
git push
git status .
